# ========================================
# ZMK Studio
# ========================================
# DYA Studio 接続機能を有効化
CONFIG_ZMK_STUDIO=y
# 接続時の&studio_unlock操作を不要に
CONFIG_ZMK_STUDIO_LOCKING=n

# ========================================
# DYA Studio モジュール (aerogu34_rightと同等設定)
# ========================================
# BLE管理
CONFIG_ZMK_BLE_MANAGEMENT=y
CONFIG_ZMK_BLE_MANAGEMENT_STUDIO_RPC=y

# バッテリー履歴
CONFIG_ZMK_BATTERY_HISTORY=y
CONFIG_ZMK_BATTERY_HISTORY_STUDIO_RPC=y
# USB接続中セッションのバッテリー履歴も記録
CONFIG_ZMK_BATTERY_SKIP_IF_USB_POWERED=n

# 設定RPC (アイドル・スリープ設定)
CONFIG_ZMK_SETTINGS_RPC=y
CONFIG_ZMK_SETTINGS_RPC_STUDIO=y

# デバイス情報 (DYA Studio トラブルシューティング)
CONFIG_ZMK_DEVICE_INFO=y
CONFIG_ZMK_DEVICE_INFO_STUDIO_RPC=y

# ウォッチドッグ (フリーズ自動回復)
CONFIG_ZMK_WATCHDOG=y

# キースイッチ診断 (チャタリング検出・キー押下統計)
CONFIG_ZMK_KSCAN_DIAGNOSTICS=y
CONFIG_ZMK_KSCAN_DIAGNOSTICS_STUDIO_RPC=y

# DYA Studio 安定化 (Studio RPC + HID同時利用時のクラッシュ防止)
CONFIG_ZMK_STUDIO_RPC_THREAD_STACK_SIZE=4096
CONFIG_ZMK_STUDIO_RPC_RX_BUF_SIZE=256
CONFIG_ZMK_STUDIO_RPC_TX_BUF_SIZE=256

# 設定永続化
CONFIG_ZMK_SETTINGS_SAVE_DEBOUNCE=1000

# ========================================
# BLE
# ========================================
# 接続安定化(実験的BLE改善: 2M PHY無効化＋セキュリティ改善を含む)
CONFIG_ZMK_BLE_EXPERIMENTAL_FEATURES=y

# TX出力+8dBm(接続のリンク確立を安定化、再接続リトライ削減)
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# スリープ復帰後などにホスト側の購読状態が食い違い入力が届かなくなる問題の定番ワークアラウンド
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n

# Device Information Service
CONFIG_BT_DIS=y

# BLE接続パラメータ(応答性優先)
# 接続間隔 7.5～15ms、ペリフェラルレイテンシ 10で応答性を改善
CONFIG_BT_PERIPHERAL_PREF_MIN_INT=6
CONFIG_BT_PERIPHERAL_PREF_MAX_INT=12
CONFIG_BT_PERIPHERAL_PREF_LATENCY=10

# ========================================
# 省電力
# ========================================
# 60分無操作でディープスリープ
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=3600000

# バッテリー報告間隔を 10分に(省電力)
CONFIG_ZMK_BATTERY_REPORT_INTERVAL=600

# ソフトオフ(保管・持ち運び時のバッテリー節約)
CONFIG_ZMK_PM_SOFT_OFF=y

# ========================================
# キースキャン
# ========================================
# 低遅延デバウンス(eager debounce): 押下は 1msで即検出、リリース側 5msでチャタリング防止
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=1
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5

# ========================================
# ポインティング
# ========================================
# マウスエミュレーション(マウスレイヤー用)
CONFIG_ZMK_POINTING=y
# 高解像度スクロール(対応ホストでスクロールが滑らかに)
CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y
